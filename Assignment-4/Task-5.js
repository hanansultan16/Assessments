// Assume variable angle1 and angle2 holds angles in radians. 
// Write a program that adds the sine of angle1 to cosine of angle2, and stores the result in variable x.

let angle1 = 30;
let angle2 = 60;

let x = Math.sin(angle1) + Math.cos(angle2);

console.log(x);

