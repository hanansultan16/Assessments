// Following statements are in English. Convert these statements into JavaScript Language code as [Just write statement]

// If age is greater than 18, print "You can vote"
// If score is greater than 60, print "Pass" otherwise print "Fail"
// If temperature is less than 0, print "Freezing"

    // First Statement
    let age = 18;
    if (age >= 18) {
        console.log("You can vote");
    }

    // Second Statement
    let score = 65;
    if (score > 60) {
        console.log("Pass");
    }

    // Third Statement
    let temperature = -5
    if (temperature < 0) {
        console.log("Freezing");
    }