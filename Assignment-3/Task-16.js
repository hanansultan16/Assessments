// Following program has error, locate the error and rewrite the code by mentioning error in comment:


let x = 10;
let y = 9;
if (x > y)
    console.log("x is greater");
else
    console.log("y is smaller");

    // x and y was not defined
    // else was not written between logs